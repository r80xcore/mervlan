# Project Progress

## MerVLAN ownership, locking, and lifecycle unification

Status: complete — Rounds 0–9 accepted under the authorized non-disruptive validation scope.

Governing contract: `dev-tools/to-do/mervlan_ownership_locking_and_lifecycle_unification_plan.md`.

Scope: execute the plan's required sequential rounds; preserve DHCP Hold, MAC
Shield, explicit terminal results, and authorization limits. No deployment,
live Update, disruptive validation, commit, or push is authorized.

Accepted execution state:

- R0: baseline matrix and focused router/local evidence recorded.
- R1: signal-safe cleanup accepted.
- R2A–R2D: canonical process identity, v2 owner records, generic lifecycle,
  caller migration, and required runtime manifests accepted.
- R3: strict action parent/self ownership, dispatcher serialization, and
  bounded manager/Save overlap accepted.
- R4: authenticated Update ownership, journal-bound boot recovery, and
  pre-mutation/pre-generation exclusivity accepted.
- R5: terminal busy/unknown/invalid-parent/cleanup outcomes accepted.
- R6: DHCP portability and crash recovery accepted.
- R7: normal maintenance and standalone Recovery interoperability accepted.
- R8: canonical observation ownership with specialized generation policy
  retained accepted.
- R9: duplicate cleanup, manifests, documentation, evidence matrix, version
  consolidation, and final integration accepted.

Final evidence: elevated Ubuntu WSL2/BusyBox full self-test all completed
with 651 passes and 0 failures; 10 local contracts and 59 BusyBox syntax checks
passed. Retained final logs: .tmp/R9-VERIFY/.

Authorization-limited validation: no live router/device/browser operations,
live Update, guarded Apply, deployment, commit, or push were authorized.
Existing unrelated worktree changes remain protected.
