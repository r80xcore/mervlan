# Lock ownership unification — consolidated finding/evidence matrix

This matrix began as the Round 0 baseline and now records the consolidated
R0–R9 implementation. It maps each finding to the current implementation,
intended owner, callers, checks, and retained validation evidence. A focused
selector is listed as `planned` only when the plan names it but the maintained
router self-test does not register it.

## Baseline evidence

Run and retain one record for the baseline before production edits:

```text
git status --short
git diff --stat
git diff
git branch --show-current
git rev-parse HEAD
for f in dev-tools/tests/local/*.sh; do sh "$f"; done
find functions settings dev-tools/tests -type f -name '*.sh' -exec sh -n {} \;
```

Router baseline selectors (verify dispatch names before execution):
`process-identity`, `lock-reclaim`, `dhcp-owners`, `dhcp-phases`,
`dhcp-crash-points`, `manager-ownership`, `post-apply`,
`observation-concurrency`, `observation-generations`, `node-worker-timeout`,
`action-lifecycle`, `update-lock-ownership`, `failure-propagation`,
`apply-observation`, `shell-syntax`. BusyBox syntax evidence is retained in
`.tmp/R9-INTEGRATION/final-busybox-syntax.log` (PASS).

Baseline evidence and retained round logs are under `.tmp/` in this checkout;
the R9 focused reruns are under `.tmp/R9-INTEGRATION/`. Raw logs remain outside
durable documentation and are referenced by path below.

## Finding matrix

| ID / finding | Affected production files | Current ownership implementation | Proposed owner after implementation | Direct callers | Nested callers | Static checks | New selector; affected selectors and local tests | Non-disruptive UI; guarded-live validation | Final evidence reference |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| **SIG-01** — Trapped INT/TERM resumes after cleanup | `functions/service-event-handler.sh`, `functions/save_settings.sh`, `functions/execute_nodes.sh`, `functions/sync_nodes.sh`, `functions/ssh_trust_action.sh`, `functions/ssh_hostkey_probe.sh`, `functions/collect_clients.sh`, `functions/collect_local_clients.sh`, `functions/mac_client_meta.sh`, `functions/update_mervlan.sh` | Top-level scripts mix `EXIT INT TERM` cleanup traps; signal paths are not uniformly terminating (the Update script has its own handler). | Each affected top-level script keeps one normal `EXIT` cleanup and terminating INT/TERM handlers; tracked children reconcile before owner cleanup. | Entry scripts above when invoked by UI/service/update/collection flows. | `functions/execute_nodes.sh` launches Save/manager/node and observation children; `functions/sync_nodes.sh` launches staged node work; `functions/ssh_trust_action.sh` launches SSH probes. | `rg -n 'trap .*EXIT INT TERM|trap .*INT|trap .*TERM' functions settings`; `sh -n` all affected scripts; PID/start signalling audit. | New `signal-termination` (completed); affected `node-worker-timeout`, `failure-propagation`, `action-lifecycle`, `shell-syntax`; local `dev-tools/tests/local/action_progress_test.sh`, `dev-tools/tests/local/action_pipeline_contract_test.sh`. | Exercise Save/Apply/Sync/trust terminal modal states without disruption; guarded live Apply/Nodes Apply only after approval, verifying signal-free cleanup and connectivity. | `See consolidated R0-R9 evidence ledger|
| **ACT-01** — Parent action ownership accepted without authentication | `settings/lib_action_lock.sh`, `functions/service-event-handler.sh`, `functions/execute_nodes.sh`, `functions/save_settings.sh`, `functions/ssh_trust_action.sh`, `settings/log_settings.sh` | Parent mode trusts exported `MERV_ACTION_LOCK_PARENT_*` context while action-lock parsing/acquisition remains duplicated around callers. | `settings/lib_action_lock.sh` is the thin policy wrapper; parent mode requires exact PID/start/nonce match and never falls back to self-acquisition. | Service handler, Execute, Save, SSH trust, and log-settings action-lock entry/leave paths. | Execute exports parent context to Save/manager/node and observation children; Save forwards it to settings-only Sync; trust resume forwards it to nested probes. | `rg -n 'MERV_ACTION_LOCK_PARENT_|merv_action_lock_(acquire|parent_owned|release)' functions settings`; inspect exact owner comparison; `sh -n`. | New `action-parent-ownership` (completed); `direct-manager-save-overlap` (completed); affected `action-lifecycle`, `manager-ownership`; local `dev-tools/tests/local/save_local_only_sync_test.sh`, `dev-tools/tests/local/settings_sync_contract_test.sh`, `dev-tools/tests/local/ssh_trust_contract_test.sh`, `dev-tools/tests/local/service_settings_contract_test.sh`. | UI Save→Sync, trust resume, metadata/key actions must retain one authenticated owner; guarded live Apply verifies child ownership and release. | `See consolidated R0-R9 evidence ledger|
| **ACT-02** — Unknown action ownership lacks terminal result | `functions/service-event-handler.sh`, `functions/save_settings.sh`, `functions/sync_nodes.sh`, `functions/execute_nodes.sh`, `functions/ssh_trust_action.sh`, `settings/lib_action_ack.sh`, `settings/lib_action_progress.sh`, `www/index.html`, `www/settings/loading_actions.json` | Busy and malformed/unknown lock states are not consistently distinguished at dispatch and may leave an accepted token without a terminal result. | Dispatcher and direct workers publish exactly one stable terminal result (`busy`, `owner-unknown`, invalid-parent, cleanup-failed) and never launch work on unknown ownership. | Service-event dispatcher and direct Save/Sync/Execute/trust entry points. | Progress/ack writers publish backend result consumed by `www/index.html` and `www/settings/loading_actions.json`; Execute children report through parent aggregation. | `rg -n 'action-lock|owner-unknown|MERV_ACTION|progress|ack' functions settings www`; inspect nonzero refusal paths; `sh -n`. | New `action-lock-failure` (completed); affected `failure-propagation`, `action-lifecycle`; local `dev-tools/tests/local/action_progress_test.sh`, `dev-tools/tests/local/loading_progress_test.sh`, `dev-tools/tests/local/action_pipeline_contract_test.sh`. | Validate busy/unknown/malformed owner UI outcomes, button/modal release, stale response rejection; guarded live only after isolated tests, no intentional live signalling. | `See consolidated R0-R9 evidence ledger|
| **LCK-01** — Generic acquisition can strand an ownerless directory | `settings/lib_mervqt.sh` (generic lock helpers to migrate), future `settings/lib_owner_lock.sh` | Existing generic acquisition can create a lock directory then fail publication/cleanup, leaving an ownerless active-looking claim. | `settings/lib_owner_lock.sh` owns atomic v2 publication, bounded publication grace, failed-claim cleanup/quarantine, and authoritative restoration; no newly created unknown claim remains. | Generic lock callers currently in `lib_mervqt.sh`; migration inventory must identify each call site. | DHCP/observation/action callers that currently reach copied generic helpers (migrated in later rounds). | `rg -n 'merv_lock_acquire|merv_lock_release|owner=|mkdir.*lock|mv.*quarantine' settings functions`; inspect temp-file/mv and exact-root cleanup; `sh -n`. | New `owner-lock-contract` and `lock-publication` (completed); affected `lock-reclaim`, `atomic-publication`; no dedicated local script (explicit inspection required). | No UI change expected; guarded live validation checks lock cleanup only after approval and isolated lock tests. | `See consolidated R0-R9 evidence ledger|
| **NON-01** — Same-process nonce generation can duplicate | `settings/lib_identity.sh`, `settings/lib_mervqt.sh`, `settings/lib_action_lock.sh`, `settings/lib_action_progress.sh`, `settings/lib_action_runtime.sh`, `settings/lib_node_jobs.sh`, `settings/lib_update_state.sh`, `functions/post_apply_worker.sh` | Multiple nonce helpers/state copies exist; command-substitution use can mutate sequence state outside the caller shell. | `settings/lib_identity.sh` is the sole normal runtime nonce/identity owner; callers use a current-shell `merv_identity_nonce_next` contract. | Action, node-job, Update, DHCP, and observation ownership code that requests nonces. | Action progress/runtime records and node/observation child contexts carry the generated nonce. | `rg -n 'merv_identity_nonce|_merv_dhcp_nonce|_merv_lock_v2_nonce|RANDOM' functions settings`; inspect command substitutions; `sh -n`. | New `nonce-uniqueness` (completed); affected `process-identity`, `lock-reclaim`, `action-lifecycle`; no dedicated local script. | UI tokens remain opaque and unique; guarded live validation verifies owner/token continuity without exposing nonce values. | `See consolidated R0-R9 evidence ledger|
| **UPD-01** — Bare Update-owner bypass is unauthenticated | `settings/lib_update_state.sh`, `functions/update_mervlan.sh`, `functions/mervlan_boot_wrap.sh`, `functions/mervlan_boot.sh`, `functions/sync_nodes.sh`, `install.sh` | `MERV_UPDATE_OWNER=1` is treated as sufficient in mutation predicates and rollback/recovery child environments. | `settings/lib_update_state.sh` validates PID/start/nonce against live maintenance ownership; bare flag authorizes nothing; boot recovery remains separate and journal-bound. | Update script and its rollback/activation children; boot wrapper and install entry points. | `update_mervlan.sh` exports Update context to `install.sh`, boot, sync, and hardware steps. | `rg -n 'MERV_UPDATE_OWNER|merv_update_mutation_blocked|MERV_UPDATE_RECOVERY' functions settings install.sh`; inspect exact owner match and journal gates; `sh -n`. | New `update-exclusivity` (completed); affected `update-lock-ownership`, `action-lifecycle`; local `dev-tools/tests/local/update_lifecycle_contract_test.sh`. | UI ordinary actions during simulated maintenance must terminate as blocked; guarded live Update validation only with approval, proving ordinary work cannot intervene. | `See consolidated R0-R9 evidence ledger|
| **UPD-02** — Ordinary entry points can bypass Update maintenance | `settings/lib_update_state.sh`, `functions/mervlan_boot_wrap.sh`, `functions/mervlan_boot.sh`, `functions/mervlan_manager.sh`, `functions/save_settings.sh`, `functions/execute_nodes.sh`, `functions/sync_nodes.sh`, `functions/heal_event.sh`, `functions/dropbear_sshkey_gen.sh`, `functions/mac_client_meta.sh`, `functions/mac_refresh.sh`, `functions/post_apply_worker.sh`, `functions/collect_clients.sh`, `functions/collect_local_clients.sh`, `functions/ssh_trust_action.sh` | Entry-point checks are uneven; ordinary mutation/observation can avoid the central Update predicate. | Central Update predicate gates every ordinary mutation and observation before mutation/generation increment; read-only status remains allowed. | Save, Apply/Execute, Sync, Heal/boot, hardware, key, metadata, MAC refresh, trust, and observation entry points. | Execute/manager children, collector workers, and post-apply generation requests inherit or recheck the maintenance state. | `rg -n 'merv_update_mutation_blocked|MERV_UPDATE_OWNER|post_apply_worker.sh|collect_(clients|local_clients)' functions settings`; compare Section 9 matrix; `sh -n`. | New `update-exclusivity` (completed); affected `post-apply`, `observation-concurrency`, `observation-generations`, `action-lifecycle`, `update-lock-ownership`; local `dev-tools/tests/local/update_lifecycle_contract_test.sh`, `dev-tools/tests/local/settings_sync_contract_test.sh`. | UI Save, Refresh Clients, MAC Shield, metadata, key, trust, and busy action are blocked with terminal state; guarded live Update sequence verifies quiescence through rollback/cleanup. | `See consolidated R0-R9 evidence ledger|
| **UPD-03** — Duplicate Update INT trap registration | `functions/update_mervlan.sh` (signal handler/trap block), `install.sh` and boot/update wrappers for audit | Update has dedicated signal handling but duplicate registration/cleanup interactions must be reconciled before edits. | One authoritative Update INT/TERM path; signal handler is registered once, clears/reconciles safely, and preserves quiescence/journal state. | `functions/update_mervlan.sh` main Update entry; installer/boot wrappers only when they invoke Update/recovery. | Update rollback and child steps launched with Update context. | `rg -n 'trap .*INT|handle_update_signal|MERV_UPDATE_OWNER' functions/update_mervlan.sh install.sh functions/mervlan_boot_wrap.sh`; `sh -n`. | No separate new selector; covered by completed `signal-termination`, `update-exclusivity`; affected `update-lock-ownership`, `shell-syntax`; local `dev-tools/tests/local/update_lifecycle_contract_test.sh`. | UI sees one terminal maintenance outcome; guarded live Update must not be interrupted intentionally (verify only isolated test fixture and static/contract evidence). | `See consolidated R0-R9 evidence ledger|
| **DHC-01** — DHCP incomplete-lock age depends on unavailable stat | `settings/lib_mervqt.sh`, `functions/update_mervlan.sh` | DHCP crash recovery uses `stat -c %Y` for incomplete-lock age; this is not ASUSWRT/BusyBox portable. | Specialized DHCP ownership remains in `settings/lib_mervqt.sh`; one verified portable timestamp helper fails closed when age is unreadable. | DHCP Hold/state-lock acquire/recover/release functions in `lib_mervqt.sh`; Update invokes DHCP-related paths during lifecycle. | Update/boot/recovery flows consume DHCP ownership and Hold state. | `rg -n 'stat -c|_mdla_mtime|merv_dhcp.*lock' settings/lib_mervqt.sh functions/update_mervlan.sh`; BusyBox syntax/tool check; `sh -n`. | New `dhcp-incomplete-lock`, `router-portability` (completed); affected `lock-reclaim`, `dhcp-owners`, `dhcp-phases`, `dhcp-crash-points`, `dhcp-rule-exactness`, `dhcp-ebtables-failures`; local `dev-tools/tests/local/update_lifecycle_contract_test.sh`. | No UI semantics change; guarded live Apply/Update verifies Hold and exact ebtables state after approved tests. | `See consolidated R0-R9 evidence ledger|
| **DHC-02** — PID-only incomplete DHCP ownership can remain protected | `settings/lib_mervqt.sh` | Incomplete DHCP lock fields may rely on PID presence and remain protected indefinitely; PID alone is not ownership. | DHCP-specific grace/recovery requires complete PID/start/created/nonce; old incomplete claims quarantine; complete live owners remain protected regardless of age. | DHCP lock acquisition/recovery helpers in `lib_mervqt.sh`. | Update, boot, Heal, and MAC Shield/Hold paths that defer to DHCP ownership. | `rg -n 'owner_pid|proc_start_time|created|owner_nonce|merv_dhcp.*lock' settings/lib_mervqt.sh`; inspect PID/start matching and age failure paths; `sh -n`. | New `dhcp-incomplete-lock` (completed); affected `lock-reclaim`, `dhcp-owners`, `dhcp-crash-points`; no dedicated local script. | UI remains fail-closed while Hold is active; guarded live Apply validates no bridge leak, Hold release ordering, and recovery of dead/reused owners. | `See consolidated R0-R9 evidence ledger|
| **DHC-03** — DHCP release can leave malformed ownership | `settings/lib_mervqt.sh` | DHCP release removes state-lock directory before proving restoration; failed `rmdir`/cleanup can leave malformed or missing ownership. | DHCP release preserves a complete owner record, restores it on removal failure, requires exact nonce, and returns failure while remaining recoverable. | `merv_dhcp_hold_release` and DHCP state-lock release helpers. | Apply/Update/Heal cleanup and MAC Shield/Hold teardown call release paths. | `rg -n 'merv_dhcp_hold_release|rmdir|owner_nonce|atomic' settings/lib_mervqt.sh`; inject removal failure; `sh -n`. | New `dhcp-incomplete-lock` (completed); affected `dhcp-crash-points`, `dhcp-owners`, `dhcp-ebtables-failures`; no dedicated local script. | UI reports cleanup failure rather than success; guarded live validation checks Hold, exact rules, and recoverable ownership after approved failure-injection fixture only. | `See consolidated R0-R9 evidence ledger|
| **MNT-01** — Normal maintenance and standalone Recovery use different owner formats | `functions/update_mervlan.sh`, `functions/mervlan_recover.sh`, `functions/mervlan_backup.sh`, future `settings/lib_owner_lock.sh` | Normal maintenance uses current Update owner state while standalone Recovery has an independent/legacy owner parser and four-field format. | Canonical v2 owner record is shared; Recovery retains only a minimal standalone copy and exact legacy four-field migration for the maintenance lock. | Update maintenance and Backup lifecycle; Recovery lock acquire/reclaim/release. | Boot recovery invokes `mervlan_recover.sh`; Update/Backup can leave locks consumed by the other path. | `rg -n 'proc_start_time|owner_nonce|format_version|mervlan_recover|mervlan_backup' functions settings`; inspect v2/legacy handling and source independence; `sh -n`. | New `maintenance-lock-interop` (completed); affected `update-lock-ownership`, `recovery`; local `dev-tools/tests/local/update_lifecycle_contract_test.sh`. | No ordinary UI change; guarded live validation only after approval verifies Update/Recovery lock interop and safe terminal cleanup. | `See consolidated R0-R9 evidence ledger|
| **OBS-01** — Observation can publish invalid start identity | `functions/post_apply_worker.sh`, `settings/lib_identity.sh` | Observation owner publication has a separate identity path and may write start time `0`/missing when lookup fails. | Shared identity and owner mechanics reject zero/missing start time before publication; observation retains generation/coalescing policy. | `post_apply_worker.sh` observation request/worker ownership functions. | `functions/collect_clients.sh`, `functions/collect_local_clients.sh`, `functions/mervlan_manager.sh`, `functions/execute_nodes.sh`, `functions/heal_event.sh` request or wait on observation. | `rg -n 'merv_identity_current_start|OBS_.*START|proc_start|owner_nonce' functions/post_apply_worker.sh settings/lib_identity.sh`; inspect atomic owner publication; `sh -n`. | New `observation-lock` (completed); affected `post-apply`, `observation-concurrency`, `observation-timeouts`, `observation-generations`, `observation-resume-progress`, `atomic-publication`, `apply-observation`, `client-refresh-contract`; no dedicated local script. | Refresh Clients and Apply observation terminal UI remains unchanged on failure; guarded live Apply verifies one coherent final generation and unchanged JSON after failure. | `See consolidated R0-R9 evidence ledger|
| **OBS-02** — Observation release can destroy ownership before rmdir | `functions/post_apply_worker.sh` | Observation release removes ownership before confirming lock-directory removal; obstruction can destroy the authoritative record. | Exact PID/start/nonce release restores the complete owner record when directory removal fails and returns failure. | `post_apply_worker.sh` observation lock acquire/release. | Manager/Execute/Heal and collectors wait for or invoke the observation worker; generation publication follows worker terminal state. | `rg -n 'obs_lock_release|rmdir|owner|heartbeat|nonce' functions/post_apply_worker.sh`; inject `rmdir` obstruction and inspect owner restoration; `sh -n`. | New `observation-lock` (completed); affected `post-apply`, `observation-concurrency`, `observation-timeouts`, `observation-generations`, `atomic-publication`, `apply-observation`; no dedicated local script. | UI must retain prior complete client JSON and show failure; guarded live Apply verifies lock recovery, DHCP/MAC Shield safety, and exactly one final observation generation. | `See consolidated R0-R9 evidence ledger|
| **PORT-01** — Legacy GUI Update-reference ledger uses stat | `functions/update_mervlan.sh`, `settings/var_settings.sh` (ledger path) | `consume_gui_update_ref` compares `stat -c %Y` metadata plus raw ref, which is unavailable/non-portable on target shells. | Update keeps one-shot/ref-content semantics using a portable validated timestamp or content hash; canonical refs and unchanged transport behaviour remain stable. | `functions/update_mervlan.sh` `consume_gui_update_ref` and Update mode dispatch. | GUI/settings writer creates `vlanmgr_update_ref`; Update lifecycle consumes it and records `MERV_UPDATE_CONSUMED_FILE`. | `rg -n 'consume_gui_update_ref|stat -c|MERV_UPDATE_CONSUMED_FILE|vlanmgr_update_ref' functions/update_mervlan.sh settings`; BusyBox command check; `sh -n`. | New `router-portability` (completed); affected `update-lock-ownership`; local `dev-tools/tests/local/update_lifecycle_contract_test.sh`. | No UI contract change beyond one-shot ref consumption; guarded live Update not required for ledger-only proof and remains approval-gated. | `See consolidated R0-R9 evidence ledger|
| **TXT-01** — Mojibake log message | `functions/execute_nodes.sh` | Error path contains mojibake (`âœ—`) in the local-manager identity failure log. | Keep the same failure path and replace the message with portable ASCII: `ERROR: Could not record local manager process identity`. | `execute_nodes.sh` local manager child identity recording path. | Execute node orchestration and parent failure propagation consume the log/result. | `rg -n 'Could not record local manager|â|mojibake' functions/execute_nodes.sh`; `sh -n`; inspect log encoding. | No new selector; affected `execute-node-runner`, `failure-propagation`, `shell-syntax`; no dedicated local script (explicit string inspection). | UI should show existing failure classification; no guarded live test needed for text-only change (include in approved validation bundle if executed). | `See consolidated R0-R9 evidence ledger|

## Consolidated R0–R9 evidence ledger

The finding rows above retain their original plan wording and selector mapping;
this ledger binds every finding to retained implementation/test evidence.

| Finding | Evidence (all PASS unless noted) |
| --- | --- |
| SIG-01 | `.tmp/R9-INTEGRATION/final-signal-termination.log` (14 assertions); `.tmp/R9-INTEGRATION/final-busybox-syntax.log` |
| ACT-01 | `.tmp/R3-VERIFY/action-parent-ownership.log` (16); `.tmp/R3-VERIFY/direct-manager-save-overlap.log` (4) |
| ACT-02 | `.tmp/R9-INTEGRATION/action-lock-failure.log`; `.tmp/r5_verify/router_tests.log` |
| LCK-01 | `.tmp/R9-INTEGRATION/final-owner-lock-contract.log` (24); `.tmp/R9-INTEGRATION/lock-publication.log` (44) |
| NON-01 | `.tmp/R9-INTEGRATION/final-nonce-uniqueness.log` (13); static sweep `.tmp/R9-INTEGRATION/final-static-sweeps.log` |
| UPD-01 | `.tmp/R9-INTEGRATION/update-exclusivity.log`; `.tmp/r7_independent_verify/update-lock-ownership-rerun2.log` |
| UPD-02 | `.tmp/R9-INTEGRATION/update-exclusivity.log`; `.tmp/R8-VERIFY/observation-generations.log` (8) |
| UPD-03 | `.tmp/R9-INTEGRATION/final-signal-termination.log`; single Update INT/TERM registration inspected |
| DHC-01 | `.tmp/R9-INTEGRATION/final-router-portability.log` (4); `.tmp/R9-INTEGRATION/final-dhcp-incomplete-lock.log` (17) |
| DHC-02 | `.tmp/R9-INTEGRATION/final-dhcp-incomplete-lock.log` (17); `.tmp/R9-INTEGRATION/rerun-dhcp-owners.log` (13) |
| DHC-03 | `.tmp/R9-INTEGRATION/rerun-dhcp-incomplete-lock.log` (17; release restoration) |
| MNT-01 | `.tmp/r7_independent_verify/maintenance-lock-interop-rerun2.log` (10); `.tmp/r7_independent_verify/recovery-rerun2.log` (22) |
| OBS-01 | `.tmp/R8-VERIFY/observation-lock.log` (16); `.tmp/R8-VERIFY/observation-generations.log` (8) |
| OBS-02 | `.tmp/R8-VERIFY/observation-lock.log` (release obstruction/restoration) |
| PORT-01 | `.tmp/R9-INTEGRATION/final-router-portability.log` (4); `.tmp/R9-INTEGRATION/final-static-sweeps.log` has no `stat -c` matches |
| TXT-01 | `.tmp/R9-INTEGRATION/final-execute-node-runner.log`; `functions/execute_nodes.sh` ASCII error inspection |

R9 integration validation: `.tmp/R9-INTEGRATION/final-*.log` and
`.tmp/R9-INTEGRATION/final-busybox-syntax.log` all exit 0. The elevated
Ubuntu/BusyBox broad selftest completed with `passes=651 failures=0`, retained
in `.tmp/R9-VERIFY/router-all.log`. Its `live-audit` selector reports that
`ebtables` is unavailable on the host; no live-router validation was
authorized. Focused logs and retained R0–R8 evidence remain acceptance
evidence.

## Round 2B compatibility-reader inventory

The canonical v2 `owner` record is authoritative. The sidecar files listed
below remain in place until their named readers are migrated in a later round;
this round does not remove or reinterpret any caller format.

| Compatibility artifact / format | Named reader(s) | Classification | Round 2B decision and focused coverage |
| --- | --- | --- | --- |
| `owner` (v2: `pid`, `proc_start_time`, `owner_nonce`, `created`, `heartbeat`) | `settings/lib_mervqt.sh`: `_merv_lock_v2_read`, `merv_lock_state`, `merv_lock_heartbeat`, `merv_lock_release`; `functions/collect_clients.sh`: `collect_execute_nodes_observation_grant_valid` | Active runtime readers | Retain as the single authoritative record. New `settings/lib_owner_lock.sh:merv_owner_v2_read` enforces the strict five-field contract; `owner-lock-contract` covers valid, malformed, exact/live, and stale-heartbeat cases. |
| Generic lock sidecars beside `owner`: `pid`, `proc_start_time`, `owner_nonce`, `created`, `heartbeat` | `settings/lib_mervqt.sh`: `_merv_lock_v2_write` publishes and `merv_lock_release` removes them; no active generic runtime reader found in the current source | Compatibility publication (writer-only at present) | Retain for the migration window; do not remove before caller audit in Round 2D/9. The v2 `owner` file remains authoritative and sidecars are not parsed by the new library. |
| Action-lock four-field `owner`: `pid`, `start`, `nonce`, `created` | `settings/lib_action_lock.sh`: `merv_action_lock_read`, `merv_action_lock_parent_owned`, release paths | Active runtime reader; separate policy format | Retain unchanged for Round 3 action-policy migration. It is not accepted by the canonical v2 parser. |
| Standalone Recovery four-field `owner`: `pid`, `start`, `nonce`, `created` | `functions/mervlan_recover.sh`: `recovery_lock_read` called by `recovery_acquire_lock` and `recovery_release_lock` (plus `recovery_lock_identity_matches` for reclaim) | Active runtime compatibility reader | Retain unchanged for Round 7 maintenance/Recovery interoperability; generic v2 parsing is not added here. |
| DHCP owner directories (`pid`, `proc_start_time`, `owner_nonce`, `created_epoch`, and phase fields) | `settings/lib_mervqt.sh`: DHCP acquire/reconcile/handoff/release helpers; `dev-tools/tests/router/mervlan_selftest.sh`: DHCP owner fixtures | Active specialized runtime and test readers | Retain as specialized DHCP ownership. Do not migrate or remove under Round 2B; DHCP selectors remain the compatibility regression coverage. |
| Legacy empty lock files | `settings/lib_mervqt.sh`: `merv_lock_quarantine_legacy_file` | Migration-only reader | Retain stale-age/quarantine behavior until generic caller migration completes; malformed or fresh legacy files remain fail-closed. |

## Evidence record template

The historical row text retains the original Round 0 `TBD` placeholders for
diff-friendly traceability; the consolidated ledger above is authoritative for
R0–R9 evidence. For a future finding, add a retained concise record containing:

```text
finding: <ID>
round: <round/iteration>
commands: <exact command(s)>
environment: <host/router/BusyBox details>
result: PASS | FAIL | PARTIAL | INCONCLUSIVE
duration: <optional>
evidence: dev-tools/evidence/<run-id>/<file>
notes: <environmental limitation, existing failure, or deferred item>
```

Do not paste complete logs here. Keep raw output under `dev-tools/evidence/`.
